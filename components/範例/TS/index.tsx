import * as React from 'react';
import classNames from 'classnames';
import './css.scss';

const Module = () => {
    const className = 'qwe';
    return <div className={classNames(className)}>qwe</div>;
};

export default Module;
